var baseurl="";$(function(){	baseurl = document.getElementById("domainKey").value;			//第一次取数据			getdata(false);			//每60秒取一次			circleData();		});		$("#contentIpt").click(function(){	if(this.value=="输入内容"){		this.value="";	}});		/**		*循环取实时数据		*/		var canCircleData = false;		function circleData(){			window.setInterval(function(){				if(canCircleData){					canCircleData = false;					getdata(true);				}			},6000);		}		/**		*isReplay 是否中查询客服回复数据 		*/		function getdata(isReplay){			if(!isReplay){				$("#loadingDiv").show();			}			var _data = {					groupId : $("#groupId").val(),					lastTime : $("#lastTime").val(),					onlyReplay: isReplay			}			$.ajax({				   type: "POST",				   url: baseurl+"/yzmm/account/feedback/detail",				   data: _data,				   dataType:"json",				   complete:function(){					   if(!isReplay){						   $("#loadingDiv").hide();						}					   //可以进行下一个循环					   canCircleData = true;				   },				   success: function(data){					   if(!data.success){						  return; 					   }					   msg = data.ext					   if(msg && msg.length){						   for(var i=0 ;i<msg.length;i++){							   var m = msg[i];							   if(m.isQuestion){								   dealQuestion(m);							   }else{								   dealReply(m);							   }						   }				     	  //这里要更新lastTime						   var m = msg[msg.length-1];							$("#lastTime").val(m.lastTime);							if(!isReplay){								$(".M_form").show();							}														$("#submitDiv").show();												   }					   				   }				});		}		//处理问题		function dealQuestion(obj){			var userInfo = $("#userInfo").attr("src");			var wf = $("#wrapFirst").clone();			wf.attr("id","");				if(userInfo){					wf.find(".wrapFirstImg").attr("src",userInfo);				}			   var content = wf.find(".wrapFirstP");			   content.html(obj.content);			   			   var time = wf.find(".dlTxt_time");			   time.html(obj.createTime);			   wf.show();			   $("#wrap").append(wf);		}		//处理回复		function dealReply(obj){			var ws = $("#wrapSecond").clone();				ws.attr("id","");			   var content = ws.find(".wrapSecondP");			   			   content.html(obj.replyContent);			   			   var time = ws.find(".dlTxt_time");			   time.html(obj.replyTime);			   ws.show();			   $("#wrap").append(ws);		}		//防重复提交		var subing = false;		//提交请求		function submitParams(){			if(subing){				toast("正在提交，请稍候...");				return;			}			if(!$("#contentIpt").val()||$("#contentIpt").val()=="输入内容"|| $("#contentIpt").val().length>=100){				toast("提交内容不可为空且长度不得大于100字");				return;			}			$("#content").val($("#contentIpt").val());			var _data = $("#paramsForm").serialize();			subing = true;			$.ajax({				   type: "POST",				   url: baseurl+"/yzmm/account/feedback/questionSubmit",				   data: _data,				   dataType:"json",				   complete:function(){					   subing = false;				   },				   success: function(msg){				     if(msg.success){				    	 $("#contentIpt").val("");				    	 dealQuestion( msg.ext);				     }else{				    	toast("提交失败，请稍候重试！");				     }				   },				   error:function(){					  toast("提交失败，请稍候重试！");				   }				});		}		